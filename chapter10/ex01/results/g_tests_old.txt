#include "studentTypeImp.cpp"
void initializeStudentType() {
    studentType student1;
    studentType student2("Brain", "Johnson", '*', 85, 95, 3.89);
}

TEST(initStudentType, 1) {
    ASSERT_NO_THROW(initializeStudentType());
}
TEST(setAndGetFirstName, 1) {
    studentType student;
    student.setFirstName("Wyatt");
    ASSERT_EQ(student.getFirstName(), "Wyatt");
}
TEST(setAndGetFirstName, 2) {
    studentType student;
    student.setFirstName("Johnny");
    ASSERT_EQ(student.getFirstName(), "Johnny");
}

TEST(setAndGetLastName, 1) {
    studentType student;
    student.setLastName("Earp");
    ASSERT_EQ(student.getLastName(), "Earp");
}
TEST(setAndGetLastName, 2) {
    studentType student;
    student.setLastName("Ringo");
    ASSERT_EQ(student.getLastName(), "Ringo");
}
TEST(setAndGetGrade, 1) {
    studentType student;
    student.setTestScore(100);
    student.setProgScore(100);
    student.setGrade();
    ASSERT_EQ(student.getGrade(), 'A');
}
TEST(setAndGetGrade, 2) {
    studentType student;
    student.setTestScore(60);
    student.setProgScore(60);
    student.setGrade();
    ASSERT_EQ(student.getGrade(), 'D');
}
TEST(setAndGetTestScore, 1) {
    studentType student;
    student.setTestScore(87);
    ASSERT_EQ(student.getTestScore(), 87);
}TEST(setAndGetTestScore, 2) {
    studentType student;
    student.setTestScore(64);
    ASSERT_EQ(student.getTestScore(), 64);
}
TEST(setAndGetProgScore, 1) {
    studentType student;
    student.setProgScore(95);
    ASSERT_EQ(student.getProgScore(), 95);
}TEST(setAndGetProgScore, 2) {
    studentType student;
    student.setProgScore(80);
    ASSERT_EQ(student.getProgScore(), 80);
}
TEST(setAndGetGPA, 1) {
    studentType student;
    student.setGPA(92.65);
    ASSERT_EQ(student.getGPA(), 92.65);
}TEST(setAndGetGPA, 2) {
    studentType student;
    student.setGPA(73.27);
    ASSERT_EQ(student.getGPA(), 73.27);
}
